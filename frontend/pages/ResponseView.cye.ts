/// <reference types="Cypress" />

export {};

describe("response view", () => {
    it("if not logged in, redirect to login", () => {});
    it("if no response, redirect to survey", () => {});
    it("if response, compare with other's", () => {});
});
